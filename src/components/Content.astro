---
export interface Props {
    className?: string;
}

const { className = '' } = Astro.props;
---

<article class={`mx-auto max-w-4xl items-center flex flex-col ${className}`}>
    <slot />
</article>